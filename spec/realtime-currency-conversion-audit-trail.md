# Spec

<!-- Business layer + Known Constraints — carried through exactly as given, not written by you -->

**Title:** Real-Time Currency Conversion & Audit Trail

**Business Idea:** Treasury operations teams at our enterprise customers
currently convert currency using manual lookups against a third-party
portal — slow, and it leaves no record for compliance. We need instant,
self-serve conversion inside our own app, and because these are regulated
transactions, every conversion must be reconstructable for an auditor on
demand, not pieced together afterward from emails or spreadsheets.

**Persona:** Treasury operations analysts at enterprise customers, who
process multiple cross-border settlements a day and are personally
accountable if a conversion can't be justified during an audit.

**User Need:** See a trustworthy converted amount the moment I enter it
— and be able to pull up any past conversion, with its rate and timestamp,
the moment an auditor asks for it.

**Known Constraints:**
- Frontend must read `VITE_API_URL` dynamically at container runtime using
  a generic entrypoint script placeholder replacement; replace the placeholder token
  `__VITE_API_URL__` inside the built `index.html` (or equivalent) with the runtime
  `VITE_API_URL` value.
- Backend must read database configurations exclusively from runtime
  environment variables.
- Backend must query external currency conversion data exclusively using a base URL from an environment variable (`CURRENCY_API_BASE_URL`), defaulting to an unauthenticated open-source endpoint like Frankfurter (`https://frankfurter.dev`).
- The application must not hardcode third-party API response schemas; utilize flexible JSON property mapping or an abstraction layer to isolate internal compliance tracking from external JSON structure changes (such as `rates` vs `conversion_rates`).
- The audit trail record must capture the exact execution timestamp generated by your backend server alongside any date or sequence markers returned by the external provider, ensuring complete sub-second audit reconstruction even if the provider only exposes a calendar date.
- External HTTP calls must handle failures gracefully with fallback values or clear domain exceptions rather than bubbling up raw network or serialization errors.
- CI/CD pipeline must inject placeholders safely at deployment time without
  exposing sensitive data in container layers.
- Restricted to specific stack versions: React (Vite/Node 24.*) and C#
  (**backend must target `net10.0` for build/test; CI must use `dotnet-version: 10.0.x`**).
- Must adhere to a strict folder layout (`src/frontend` and `src/backend`).
- A .NET solution file must always be generated at `src/backend/OuterloopLabApi.sln` via `dotnet new sln` + `dotnet sln add`, with the backend project added to it, and CI/build/test commands must invoke this `.sln` rather than the `.csproj` directly.
- Deploy as a single Azure Container App using the sidecar pattern to host
  both frontend and backend.
- Authenticate to Cosmos DB exclusively using token-based authentication via the pre-assigned User-Assigned Managed Identity, instantiated using `DefaultAzureCredential` configured with the specific `ManagedIdentityClientId`. Absolutely zero connection strings, master keys, or account keys are permitted in code, configurations, or logs.
- Utilize `Microsoft.Azure.Cosmos` and `Azure.Identity` packages for
  secure, token-based database interactions, including data-plane CRUD operations like adding and deleting items.
- Include `Azure.ResourceManager` and `Azure.ResourceManager.CosmosDB` packages to programmatically provision the Cosmos DB database and container via the control plane inside the application startup lifecycle (`Program.cs`) before the web application runs, utilizing the same token-based Managed Identity credentials for authentication.
  - ARM provisioning is best-effort; Managed Identity RBAC for ARM may differ from data-plane RBAC.
  - CRUD (and data-plane creation if needed) must be done via the Cosmos SDK (data-plane) using token-based Managed Identity credentials; access keys must not be used.
  - After ARM attempt, startup must run token-authenticated data-plane `CreateDatabaseIfNotExistsAsync` and `CreateContainerIfNotExistsAsync` and only proceed if the database/container are available (either already exist or were created). If token-authenticated data-plane create-if-not-exists fails, startup must fail.
- Map and bind all configuration values, including `AZURE_MANAGED_IDENTITY_CLIENT_ID`, endpoint URLs, and resource names, directly from the environment variables **using the exact keys** defined in `docs\CONTAINER_ENVIRONMENT_VARIABLES.md` without using local settings configuration files as fallbacks.

<!-- Technical layer — this is what you add -->

**Current Context:** The implementation should center on exact paths under the required layout: `src/backend/Program.cs`, `src/backend/OuterloopLabApi.sln`, and a backend web API project assumed at `src/backend/OuterloopLabApi/OuterloopLabApi.csproj`, with feature code added under exact subpaths such as `src/backend/OuterloopLabApi/Models/`, `src/backend/OuterloopLabApi/Services/`, and `src/backend/OuterloopLabApi/Repositories/`. On the frontend, the implementation should update `src/frontend/index.html`, `src/frontend/package.json`, and the main app entry files at `src/frontend/src/main.tsx` or `src/frontend/src/main.jsx` plus `src/frontend/src/App.tsx` or `src/frontend/src/App.jsx`; if any of those app files are missing, create them at those exact paths rather than introducing a different folder layout.

**In Scope:**
- Add a backend `POST /api/conversions` endpoint that accepts a single conversion request with source currency, target currency, and amount, fetches the latest rate from the configured provider, calculates the converted amount, and persists an immutable audit record.
- Add a backend `GET /api/conversions/{id}` endpoint that returns the stored audit record needed for compliance lookup, including the original request values, applied rate, provider marker values, and backend execution timestamp.
- Add backend startup and service wiring so the conversion feature uses a dedicated provider client abstraction, Cosmos-backed persistence, and token-authenticated startup readiness for the audit container before serving requests.
- Add a frontend form in `src/frontend/src/App.tsx` or `src/frontend/src/App.jsx` that lets the user enter amount, source currency, and target currency, submit the conversion request, and immediately display the converted amount, audit identifier, rate, provider date/marker, and backend execution timestamp.
- Add a frontend audit lookup flow that retrieves a prior conversion by audit identifier and renders the same compliance fields without re-calling the external provider.

**Out of Scope:**
- Bulk or batch currency conversions in a single request.
- Historical trend charts, dashboards, or reporting exports.
- Editing or deleting audit records after creation.
- User authentication, authorization, or persona-specific access control changes.
- Rate caching, retry dashboards, or provider failover across multiple third-party services.

**Feature Constraints:**
- Use `IHttpClientFactory` for the external currency provider client and keep provider-specific JSON parsing behind a dedicated adapter/service so controller and persistence code never depend on provider field names directly.
- Validate request payloads so `amount` must be greater than zero and `sourceCurrency`/`targetCurrency` must be uppercase 3-letter ISO-style currency codes; invalid input must return `400` with `ProblemDetails`.
- Use `decimal` for monetary values end-to-end in request models, domain models, persistence models, and response DTOs to avoid floating-point drift.
- Persist each audit document as write-once data containing at minimum: audit id, source currency, target currency, original amount, converted amount, applied rate, provider base currency if supplied, provider date or sequence marker if supplied, and the backend-generated UTC execution timestamp with sub-second precision.
- When the provider call fails, times out, or returns unusable data, translate that failure into a domain-safe `503` `ProblemDetails` response with no raw exception text, stack trace, or upstream payload leaked to the client.

**Expected Outcome:** A user can enter a live conversion request and immediately receive both a calculated result and a durable audit identifier. For example, if the user submits `POST /api/conversions` with `{ "sourceCurrency": "USD", "targetCurrency": "EUR", "amount": 100.00 }` and the provider adapter resolves an applied rate of `0.92` with provider date `2026-08-01`, the API returns `200` with a body containing the original amount `100.00`, converted amount `92.00`, rate `0.92`, a non-empty audit id, the provider date `2026-08-01`, and a backend execution timestamp like `2026-08-01T12:34:56.789Z`. A later `GET /api/conversions/{auditId}` returns the same persisted values without recalculating from the provider. If the provider is unavailable, the create endpoint returns a `503` `application/problem+json` response whose title clearly states that the currency conversion provider is unavailable.

**Expected Agent Output:** OpenCode should produce a concrete code change set under `src/backend` and `src/frontend` that implements the two API endpoints, Cosmos-backed audit persistence, provider adapter/service wiring, runtime frontend API URL usage, and the UI for conversion plus audit lookup, along with any required tests or validation code and a verification summary listing the exact build/test commands run, using `src/backend/OuterloopLabApi.sln` for all .NET commands.

**Suggested Intent For OpenCode:** Implement the spec in `spec/realtime-currency-conversion-audit-trail.md` across `src/backend` and `src/frontend`, and strictly preserve every Known Constraint exactly as written, including the required folder layout, `net10.0` backend target, `src/backend/OuterloopLabApi.sln` usage for build/test, runtime-only environment variable configuration using the exact keys from `docs\CONTAINER_ENVIRONMENT_VARIABLES.md`, managed-identity-only Cosmos access and startup provisioning behavior, runtime `VITE_API_URL` placeholder replacement, and graceful provider error handling; then add the minimal code, validation, persistence, and UI needed for `POST /api/conversions`, `GET /api/conversions/{id}`, and frontend conversion/audit lookup, and verify the implementation with the relevant frontend and solution-level backend commands.
